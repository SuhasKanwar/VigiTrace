[pytest]
testpaths = tests
# The service is laid out as top-level packages (models, vendors, services),
# so the service directory itself has to be importable no matter where pytest
# was invoked from.
pythonpath = .
addopts = -ra
