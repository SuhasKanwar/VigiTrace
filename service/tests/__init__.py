"""VigiTrace service test suite.

The package carries ``__init__.py`` files so pytest imports the modules under
their dotted package name; that keeps ``tests.mock_dvr`` importable from every
test module without any sys.path manipulation inside the tests themselves.
"""
